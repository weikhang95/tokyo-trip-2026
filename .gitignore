# Insurance / personal documents — repo is public, keep these local only
*.pdf
Certificate*
certificate*
*保单*
*policy*.pdf

# Passport / booking confirmations
*passport*
*boarding*
*e-ticket*

# OS
.DS_Store

# Agent metadata
.agent/
.agents/

# MCP server config — contains GOOGLE_MAPS_API_KEY in plaintext
# glob, not the bare name: a .bak of this file was committed once by `git add -A`
.mcp.json*

# Local env vars — repo is public
.env
.env.*
